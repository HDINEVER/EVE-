{
  "rules": [
    {
      "allowed": {
        "origins": ["http://localhost:*"],
        "methods": ["GET", "HEAD"],
        "headers": ["*", "Range"]
      },
      "exposeHeaders": ["Accept-Ranges", "Content-Length", "Content-Range", "ETag"],
      "maxAgeSeconds": 3600
    }
  ]
}
